const development = {
  name: "development",
  PORT: 8000,
  asset_path: "public",
  mongodb_URI: "mongodb://localhost/issue-tracker",
};

const environment = development;
module.exports = environment;
